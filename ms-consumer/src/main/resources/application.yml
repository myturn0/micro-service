server:
  port: 8001
spring:
  application:
    name: j-cloud-consumer

# eureka 配置
eureka:
  client:
    serviceUrl:
      defaultZone: http://jorian:123456@localhost:8761/eureka
  instance:
    instance-id: ms-consumer8001

logging:
  level:
    root: INFO

info:
  app.name: micro-service
  company.name: lyy
  build.artifactId: "@project.artifactId@"
  build.version: "@project.version@"
